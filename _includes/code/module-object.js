define(function(){

    return {

        // load method, required when no constructor available
        load:function(element,options) {

        },

        // options (optional)
        options:{
            'foo':'bar'
        },

        // unload (optional)
        unload:function() {

        }
    };

});